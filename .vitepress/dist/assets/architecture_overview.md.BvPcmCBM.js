import{c as n,$ as o,Q as a,j as r,m as s,h as l,ax as i,n as c,o as p,S as d}from"./chunks/framework.BqPkuuVc.js";const b=JSON.parse('{"title":"Architecture Overview","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/overview.md","filePath":"architecture/overview.md"}'),h={name:"architecture/overview.md"};function g(u,e,y,m,P,E){const t=o("Mermaid");return a(),r("div",null,[e[1]||(e[1]=s(`<h1 id="architecture-overview" tabindex="-1">Architecture Overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to &quot;Architecture Overview&quot;">​</a></h1><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>a2e/schema.py                    — A2EHostConfig (top-level config)</span></span>
<span class="line"><span>a2e/caps/base/protocol.py         — A2EMessage, MessageType, Handshake*, A2ECapability</span></span>
<span class="line"><span>a2e/core/plugins/interface.py     — A2EPlugin ABC</span></span>
<span class="line"><span>a2e/core/plugins/registry.py      — PluginRegistry</span></span>
<span class="line"><span>a2e/core/plugins/schema.py        — PluginConfig, PluginMeta</span></span>
<span class="line"><span>a2e/core/client/client.py        — A2EClient</span></span>
<span class="line"><span>a2e/core/server/server.py         — A2EServer (FastAPI + Direct)</span></span>
<span class="line"><span>a2e/core/server/executor.py      — A2EServerRuntimeExecutor (dispatch core)</span></span>
<span class="line"><span>a2e/core/server/session.py        — Session</span></span>
<span class="line"><span>a2e/core/server/session_manager.py— SessionManager</span></span>
<span class="line"><span>a2e/core/transports/base.py       — BaseTransport ABC</span></span>
<span class="line"><span>a2e/core/transports/direct.py     — DirectTransport (in-process)</span></span>
<span class="line"><span>a2e/core/transports/http.py       — HTTPTransport (POST + SSE)</span></span></code></pre></div><h2 id="system-diagram" tabindex="-1">System Diagram <a class="header-anchor" href="#system-diagram" aria-label="Permalink to &quot;System Diagram&quot;">​</a></h2>`,3)),(a(),l(d,null,{default:i(()=>[p(t,{id:"mermaid-7",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20A%5BAgent%5D%20--%3E%7CNDJSON%7C%20C%5BA2EClient%5D%0A%20%20%20%20C%20--%3E%7CPOST%20%2Fsend%7C%20T%5BTransport%20Layer%5D%0A%20%20%20%20T%20--%3E%7CSSE%20%2Fstream%7C%20C%0A%20%20%20%20T%20--%3E%7Cdeliver%7C%20S%5BA2EServer%5D%0A%20%20%20%20S%20--%3E%20E%5BA2EServerRuntimeExecutor%5D%0A%20%20%20%20E%20--%3E%7Cdispatch%7C%20PR%5BPluginRegistry%5D%0A%20%20%20%20E%20--%3E%7Croute%7C%20CR%5BCapabilityRegistry%5D%0A%0A%20%20%20%20PR%20--%3E%20P1%5BToolPlugin%5D%0A%20%20%20%20PR%20--%3E%20P2%5BMemoryPlugin%5D%0A%20%20%20%20PR%20--%3E%20P3%5BEnvPlugin%5D%0A%20%20%20%20PR%20--%3E%20P4%5BProcPlugin%5D%0A%20%20%20%20PR%20--%3E%20P5%5BLearnPlugin%5D%0A%20%20%20%20PR%20--%3E%20P6%5BSkillPlugin%5D%0A%20%20%20%20PR%20--%3E%20P7%5BToolkitPlugin%5D%0A%20%20%20%20PR%20--%3E%20P8%5BChainPlugin%5D%0A%20%20%20%20PR%20--%3E%20P9%5BMCPPlugin%5D%0A%0A%20%20%20%20E%20--%3E%7Caudit%7C%20AL%5BAuditLog%5D%0A%20%20%20%20E%20--%3E%7Cpersist%7C%20SS%5BSnapshotStore%5D%0A"})]),fallback:i(()=>[...e[0]||(e[0]=[c(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=s(`<h2 id="three-layer-design" tabindex="-1">Three-Layer Design <a class="header-anchor" href="#three-layer-design" aria-label="Permalink to &quot;Three-Layer Design&quot;">​</a></h2><p>A2E is organized into three clean layers, each with a single responsibility:</p><h3 id="_1-protocol-layer-—-schemas-and-messages" tabindex="-1">1. Protocol Layer — Schemas and Messages <a class="header-anchor" href="#_1-protocol-layer-—-schemas-and-messages" aria-label="Permalink to &quot;1. Protocol Layer — Schemas and Messages&quot;">​</a></h3><p>The wire protocol is defined entirely through Pydantic models. Every message is an <code>A2EMessage</code> with a <code>type</code> field (e.g. <code>tool/call/req</code>), a unique <code>id</code>, a protocol <code>version</code>, and a timestamp. Capability namespaces extend this base with their own request/response/event models and a <code>TYPE_MAP</code> dict that maps type strings to classes.</p><p><strong>Key files</strong>: <code>a2e/caps/base/protocol.py</code>, <code>a2e/caps/*/protocol.py</code></p><h3 id="_2-runtime-layer-—-server-executor-client" tabindex="-1">2. Runtime Layer — Server, Executor, Client <a class="header-anchor" href="#_2-runtime-layer-—-server-executor-client" aria-label="Permalink to &quot;2. Runtime Layer — Server, Executor, Client&quot;">​</a></h3><p>The runtime orchestrates message flow. The <code>A2EServer</code> accepts connections (HTTP or Direct), creates <code>Session</code> objects, and routes messages through the <code>A2EServerRuntimeExecutor</code>. The executor decodes NDJSON lines, dispatches to plugins based on the type registry, and sends responses back through the transport. The <code>A2EClient</code> provides an RPC interface with queue-based request correlation and event streaming.</p><p><strong>Key files</strong>: <code>a2e/core/server/</code>, <code>a2e/core/client/</code>, <code>a2e/core/transports/</code></p><h3 id="_3-capability-layer-—-9-plugin-namespaces" tabindex="-1">3. Capability Layer — 9 Plugin Namespaces <a class="header-anchor" href="#_3-capability-layer-—-9-plugin-namespaces" aria-label="Permalink to &quot;3. Capability Layer — 9 Plugin Namespaces&quot;">​</a></h3><p>All capability logic lives in plugins. Each namespace follows the same 3-file pattern:</p><ul><li><code>protocol.py</code> — Pydantic message models + <code>TYPE_MAP</code></li><li><code>plugin.py</code> — <code>A2EPlugin</code> subclass with abstract hooks</li><li><code>client.py</code> — High-level <code>*API</code> class wrapping <code>A2EClient</code></li></ul><p><strong>Key directories</strong>: <code>a2e/caps/tools/</code>, <code>a2e/caps/memory/</code>, <code>a2e/caps/env/</code>, <code>a2e/caps/proc/</code>, <code>a2e/caps/learn/</code>, <code>a2e/caps/skills/</code>, <code>a2e/caps/toolkits/</code>, <code>a2e/caps/chains/</code>, <code>a2e/caps/mcp/</code></p><h2 id="key-design-patterns" tabindex="-1">Key Design Patterns <a class="header-anchor" href="#key-design-patterns" aria-label="Permalink to &quot;Key Design Patterns&quot;">​</a></h2><h3 id="plugin-centric-philosophy" tabindex="-1">Plugin-Centric Philosophy <a class="header-anchor" href="#plugin-centric-philosophy" aria-label="Permalink to &quot;Plugin-Centric Philosophy&quot;">​</a></h3><p>The host is a <strong>thin execution kernel</strong> — it loads, routes, and manages lifecycle. All capability-specific logic lives in dynamically loaded plugins. This means you can add new capabilities without touching the core runtime.</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Plugin loading in A2EServerRuntimeExecutor._load_plugins()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mod </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> importlib.import_module(cls_path)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># e.g. &quot;a2e.caps.tools.plugin&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cls</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> getattr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mod, class_name)                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># e.g. &quot;ToolPlugin&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">plugin </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">plugin.setup(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, plugin_config)             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Inject config + audit_log</span></span></code></pre></div><h3 id="type-registry-pattern" tabindex="-1">Type Registry Pattern <a class="header-anchor" href="#type-registry-pattern" aria-label="Permalink to &quot;Type Registry Pattern&quot;">​</a></h3><p>Both client and server maintain a <code>type_registry</code> dict mapping <code>&quot;namespace/verb&quot;</code> strings to Pydantic model classes. Plugins extend this at startup via <code>supported_messages()</code>. This enables polymorphic decoding of NDJSON lines — the executor looks up the model class by the <code>type</code> field, then validates and instantiates.</p><h3 id="transport-abstraction" tabindex="-1">Transport Abstraction <a class="header-anchor" href="#transport-abstraction" aria-label="Permalink to &quot;Transport Abstraction&quot;">​</a></h3><p><code>BaseTransport</code> defines <code>start()</code>, <code>send()</code>, <code>deliver()</code>, and handler slots. Two implementations:</p><ul><li><strong>DirectTransport</strong> — in-memory queues, cross-wired for local testing and RL step loops</li><li><strong>HTTPTransport</strong> — POST /send + GET /stream (SSE) with session management, reconnection, and retry logic</li></ul><h3 id="session-per-connection-model" tabindex="-1">Session-Per-Connection Model <a class="header-anchor" href="#session-per-connection-model" aria-label="Permalink to &quot;Session-Per-Connection Model&quot;">​</a></h3><p>Each connection gets its own <code>Session</code> with an isolated <code>A2EServerRuntimeExecutor</code> and <code>DirectTransport</code>. HTTP mode uses <code>SessionManager</code> to create sessions on POST /session and wire SSE streams.</p><h3 id="rpc-with-event-streaming" tabindex="-1">RPC with Event Streaming <a class="header-anchor" href="#rpc-with-event-streaming" aria-label="Permalink to &quot;RPC with Event Streaming&quot;">​</a></h3><p>Client <code>rpc()</code> supports a progressive <code>event_callback</code> that receives <code>A2EEvent</code> messages (progress, artifact, log, status) before the final response. This enables streaming UI updates while waiting for long-running operations.</p><h3 id="priority-based-dispatch" tabindex="-1">Priority-Based Dispatch <a class="header-anchor" href="#priority-based-dispatch" aria-label="Permalink to &quot;Priority-Based Dispatch&quot;">​</a></h3><p>Plugins declare a <code>priority</code> (int, default 0). When a message type has multiple handlers, the executor sorts by priority. <strong>Exclusive</strong> plugins get sole handling; non-exclusive plugins broadcast to all registered handlers.</p><h2 id="opinionated-analysis" tabindex="-1">Opinionated Analysis <a class="header-anchor" href="#opinionated-analysis" aria-label="Permalink to &quot;Opinionated Analysis&quot;">​</a></h2><p><strong>Strengths:</strong></p><ol><li>Clean separation of concerns — protocol, runtime, and capabilities are fully decoupled</li><li>Extensible without touching core — new capabilities are just plugins with a <code>TYPE_MAP</code></li><li>Type-safe throughout — Pydantic v2 provides validation, serialization, and JSON schema generation</li><li>Transport-agnostic — the same client/server code works over HTTP or in-process</li><li>Audit by default — every plugin handler records timing, byte sizes, and success/error</li></ol><p><strong>Risks:</strong></p><ol><li>No authentication beyond a single <code>auth_token</code> — no OAuth, no mTLS, no RBAC</li><li>No test suite found in the repository — critical for a protocol implementation</li><li>Some cookbook tools have security issues (<code>python_eval_tool</code> uses bare <code>exec()</code>)</li><li>Debug artifacts in cookbook files (<code>pdb.set_trace()</code> left in <code>mcp_api.py</code>)</li></ol>`,32))])}const v=n(h,[["render",g]]);export{b as __pageData,v as default};
